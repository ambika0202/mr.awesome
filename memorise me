#include <stdio.h>

int main(){
int n, arr[10000], q, i, v;
scanf("%d", &n);
for(i=0;i<n;i++)
{
scanf("%d", &v);
arr[v]++;
}
scanf("%d", &q);
for(i=0;i<q;i++)
{
scanf("%d", &v);
if(arr[v]>0)
printf("%d\n", arr[v]);
else
printf("NOT PRESENT\n");
}
return 0;
}
